# Write an autocomplete class that returns all dictionary words with a given prefix

# class that has a dictionary and a method that, with an input prefix, returns a list of all words with that prefix

class Autocomplete:
    def __init__(self, dictionary):
        self.dictionary = dictionary
        return
    def get_words(self, 
