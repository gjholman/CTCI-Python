# Given a number (unsigned int n) and another number (unsigned int r),
# rotate the bits of n by r bits. 

# Essentially we are creating a function that is a circular shift
# When we shift we are assuming a shift to the right

# rotate(0x0FFFF0000, 8) = 0x0FFFF000


